import static org.junit.jupiter.api.Assertions.assertEquals;

import org.junit.jupiter.api.Test;

import dev.arturo.models.Multiply;

public class TableTest {

    Multiply testing = new Multiply(5);
    @Test
    void testMain() {
    assertEquals(5, STR);
    }
}
